import 'package:flutter/material.dart';

class RDVPage extends StatelessWidget {
  const RDVPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('RDV Page'),
      ),
      // Pas de body, donc la page sera vide
    );
  }
}
